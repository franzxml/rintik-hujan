# Enable rewriting
<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Serve index.php as default
    DirectoryIndex index.php
    
    # Prevent directory listing
    Options -Indexes
</IfModule>